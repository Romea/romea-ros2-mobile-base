/**:
  ros__parameters:
    joystick_mapping:
      axes:
        linear_speed: 0
        steering_angle: 1
      buttons:
        slow_mode: 0
        turbo_mode: 1
    cmd_output:
      message_type: geometry_msgs/Twist
#      message_priority: 100 # [0 255] if not declared no subscription to cmd_mux is done
    cmd_range:
      maximal_linear_speed:
        slow_mode: 1.0
        turbo_mode: 2.0
      maximal_steering_angle: 3.0
